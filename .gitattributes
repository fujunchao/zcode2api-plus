# 部署腳本必須保持 LF，否則 bash 在 Linux 上會因 CRLF 執行失敗
deploy/*.sh text eol=lf
deploy/zcode2api.service text eol=lf
deploy/zcode2api.env.example text eol=lf

# 容器化文件保持 LF：Dockerfile 的 `\` 續行在 CRLF 下可能被解析錯
Dockerfile text eol=lf
docker-compose.yml text eol=lf
.dockerignore text eol=lf
