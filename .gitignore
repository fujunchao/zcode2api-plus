node_modules/
js-code/node_modules/
# 管理後台 SPA（frontend/ 的依賴；dist 由 go:embed 引用，必須入庫）
frontend/node_modules/
# Python
__pycache__/
*.py[cod]
.venv/
venv/
# 运行时数据（账号 SQLite 等），绝不可入库
data/
data_test/

.env
js-code/

# 本机部署与反查临时文件
/temp/
/nginx.conf
.pytest_cache/

# 协作 agent 的本地记忆与工作区状态（含路径、调试结论等本机信息，不入库）
.workbuddy/

# 只读参照用的第三方仓库克隆（本身是独立 git 仓库，不得作为 gitlink 混入本仓库）
/zcode-switch/

# release 工作流生成的发版说明副本（源文件在 docs/releases/）
/release-notes.md
